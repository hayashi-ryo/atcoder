#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {
    int64_t N=0,K=0;
    cin >> N >> K;
    vector <string> vec_LR(N);
    vector <int64_t> vec_k(N);
    for (int i=0;i<K;i++) {
        cin >>vec_LR.at(i);
        cin >>vec_k.at(i);
    }
    int64_t sum=1;
    
    for (int i=0;i<N;i++) {
        
    }

}