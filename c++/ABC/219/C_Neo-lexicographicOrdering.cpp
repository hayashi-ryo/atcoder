#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main() {
    string X;
    cin >> X;
    vector<int> pos(26);
    for (int i=0;i<26;i++) {
        pos[X[i]-'1'] = i;
    }
    int N=0;
    cin >> N;
    vector<vector<int>> S(N);
    for (int i=0;i<N;i++) {
        stirng s;
        cin >> s;
        
    }
}