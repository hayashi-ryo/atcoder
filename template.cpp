#include <iostream>
#include <vector>
using namespace std;

int main() {
    // 型定義
    int a=0;
    int64_t b=0;

    // 配列
        // 定義
        vector<int> s(N);
        // 追加
        for (int i=0;i<N;i++) {
        cin >> s.at(i);}
        // ソート
        sort(s.rbegin(),s.rend());
}