#include <bits/stdc++.h>
using namespace std;
using ll = long long;

#define rep(i, x) for (int i = 0; i < (x); i++)

int main()
{
  // 入力
  int N, M;
  cin >> N >> M;
  vector<int> A(M), B(M), C(M);
  rep(i, M) cin >> A[i] >> B[i] >> C[i];
  // 処理
  // 出力
  return 0;
}