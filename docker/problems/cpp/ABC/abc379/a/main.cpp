#include <bits/stdc++.h>
using namespace std;
using ll = long long;

#define rep(i, x) for (int i = 0; i < (x); i++)

int main()
{
  string N;
  cin >> N;
  char a = N[0], b = N[1], c = N[2];

  cout << b << c << a << " " << c << a << b << endl;
  return 0;
}